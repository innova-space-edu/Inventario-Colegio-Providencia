# Recursos públicos

El archivo `colegio-logo.jpg` es el logo usado por el panel lateral y la pantalla de acceso. Para cambiarlo, reemplaza ese archivo manteniendo exactamente el mismo nombre. Se recomienda una imagen cuadrada o casi cuadrada, con fondo limpio y al menos 256×256 px.
