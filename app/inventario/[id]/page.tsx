import Link from "next/link";
import { notFound } from "next/navigation";
import { AppShell } from "@/components/app-shell";
import { disposeAsset } from "@/app/inventario/actions";
import { requireAdmin } from "@/lib/auth/require-admin";

type Relation<T> = T | T[] | null;
function relationOne<T>(value: Relation<T>): T | null { return Array.isArray(value) ? value[0] ?? null : value; }
function valueOrDash(value: string | number | null | undefined) { return value === null || value === undefined || value === "" ? "—" : String(value); }
function formatDate(value: string) { return new Intl.DateTimeFormat("es-CL", { dateStyle: "medium", timeStyle: "short" }).format(new Date(value)); }
export const dynamic = "force-dynamic";

export default async function AssetDetailPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params; const { supabase, profile } = await requireAdmin(); const { data: rawAsset } = await supabase.from("assets").select("*,family:asset_families(code,name),status:asset_statuses(code,name),location:locations(name,area)").eq("id", id).maybeSingle(); if (!rawAsset) notFound();
  const asset = rawAsset as typeof rawAsset & { family: Relation<{ code: string; name: string }>; status: Relation<{ code: string; name: string }>; location: Relation<{ name: string; area: string | null }> }; const family = relationOne(asset.family); const status = relationOne(asset.status); const location = relationOne(asset.location);
  let detailRows: Array<[string, string | null]> = [];
  if (family?.code === "computer") { const { data } = await supabase.from("computer_details").select("memory,storage,screen,keyboard,battery,charger").eq("asset_id", id).maybeSingle(); if (data) detailRows = [["Memoria RAM", data.memory], ["Disco / almacenamiento", data.storage], ["Pantalla", data.screen], ["Teclado", data.keyboard], ["Batería", data.battery], ["Cargador", data.charger]]; }
  else if (family?.code === "projector") { const { data } = await supabase.from("projector_details").select("lumens,hdmi,vga").eq("asset_id", id).maybeSingle(); if (data) detailRows = [["Lúmenes", data.lumens], ["HDMI", data.hdmi], ["VGA", data.vga]]; }
  else if (family?.code === "television") { const { data } = await supabase.from("television_details").select("size").eq("asset_id", id).maybeSingle(); if (data) detailRows = [["Tamaño", data.size]]; }
  const [{ data: history }, { data: disposals }] = await Promise.all([supabase.from("asset_history").select("id,event_type,description,created_at").eq("asset_id", id).order("created_at", { ascending: false }).limit(20), supabase.from("asset_disposals").select("id,disposal_date,reason,observations,approved_by,created_at").eq("asset_id", id).order("created_at", { ascending: false }).limit(10)]);
  return <AppShell><header className="topbar"><div><h1>{asset.inventory_code || asset.name || "Activo"}</h1><p>{family?.name || "Sin familia"} · actualizado {formatDate(asset.updated_at)}</p></div><div className="header-actions"><Link className="button button-ghost" href="/inventario">Volver</Link><Link className="button button-primary" href={`/inventario/${id}/editar`}>Editar</Link></div></header><section className="panel"><div className="panel-heading"><div><h2>Ficha del activo</h2><p className="muted">Información general y trazabilidad actual.</p></div><span className={`status-pill ${asset.is_disposed ? "status-danger" : ""}`}>{asset.is_disposed ? "Dado de baja" : status?.name || "Sin estado"}</span></div><div className="detail-grid"><div className="detail-item"><span>Código</span><strong>{valueOrDash(asset.inventory_code)}</strong></div><div className="detail-item"><span>Nombre</span><strong>{valueOrDash(asset.name)}</strong></div><div className="detail-item"><span>Tipo / subfamilia</span><strong>{valueOrDash(asset.asset_type)}</strong></div><div className="detail-item"><span>Familia tecnológica</span><strong>{valueOrDash(family?.name)}</strong></div><div className="detail-item"><span>Marca</span><strong>{valueOrDash(asset.brand)}</strong></div><div className="detail-item"><span>Modelo</span><strong>{valueOrDash(asset.model)}</strong></div><div className="detail-item"><span>Serie</span><strong>{valueOrDash(asset.serial_number)}</strong></div><div className="detail-item"><span>Cantidad</span><strong>{asset.quantity}</strong></div><div className="detail-item"><span>Ubicación</span><strong>{valueOrDash(location?.name)}</strong></div><div className="detail-item"><span>Área</span><strong>{valueOrDash(asset.area || location?.area)}</strong></div><div className="detail-item detail-wide"><span>Observaciones</span><strong>{valueOrDash(asset.observations)}</strong></div></div></section>{detailRows.length ? <section className="panel"><h2>Especificaciones de {family?.name.toLowerCase()}</h2><div className="detail-grid">{detailRows.map(([label, value]) => <div className="detail-item" key={label}><span>{label}</span><strong>{valueOrDash(value)}</strong></div>)}</div></section> : null}<section className="panel"><div className="panel-heading"><div><h2>Historial</h2><p className="muted">Eventos funcionales registrados para este activo.</p></div></div>{history?.length ? <div className="timeline">{history.map((event) => <article key={event.id}><div><strong>{event.event_type}</strong><span>{event.description || "Sin descripción"}</span></div><time>{formatDate(event.created_at)}</time></article>)}</div> : <div className="empty-state">Todavía no hay eventos de historial.</div>}</section>{disposals?.length ? <section className="panel"><h2>Antecedentes de baja</h2><div className="timeline">{disposals.map((item) => <article key={item.id}><div><strong>{item.disposal_date}</strong><span>{item.reason || "Sin motivo"}{item.approved_by ? ` · Autoriza: ${item.approved_by}` : ""}</span></div><time>{formatDate(item.created_at)}</time></article>)}</div></section> : null}{!asset.is_disposed ? <section className="panel danger-panel"><div className="panel-heading"><div><h2>Dar de baja</h2><p className="muted">La baja no elimina el activo: conserva ficha, historial y evidencia de la operación.</p></div></div><form action={disposeAsset} className="form-grid"><input name="asset_id" type="hidden" value={id} /><label className="field field-wide"><span>Motivo de la baja</span><input name="reason" placeholder="Ej. equipo irreparable, obsolescencia, pérdida" required /></label><label className="field"><span>Autorizado por</span><input defaultValue={profile.email} name="approved_by" /></label><label className="field field-wide"><span>Observaciones</span><textarea name="disposal_observations" rows={3} /></label><div className="form-actions field-wide"><button className="button button-danger" type="submit">Confirmar baja</button></div></form></section> : null}</AppShell>;
}
